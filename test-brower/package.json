{
  "name": "test-brower",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev-backend": "cd backend && npm install && npm run dev",
    "dev-frontend": "npm install && vite",
    "dev": "concurrently --kill-others \"npm run dev-frontend\" \"npm run dev-backend\"",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "xhr-fake": "^1.0.9"
  },
  "devDependencies": {
    "@types/react": "^18.0.17",
    "@types/react-dom": "^18.0.6",
    "@vitejs/plugin-react": "^2.1.0",
    "concurrently": "^7.4.0",
    "typescript": "^4.6.4",
    "vite": "^3.1.0"
  }
}
